name: example
description: Example app demonstrating flutter_advanced_cards package
version: 1.0.0+1

environment:
  sdk: '^3.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_advanced_cards:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true